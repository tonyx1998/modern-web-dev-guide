# React 19 peer-dependency tolerance — @codesandbox/sandpack-react (and some
# Docusaurus transitive deps) still declare React <19 peers but work with 19.
# This lets both `npm install` and CI's `npm ci` resolve the tree.
legacy-peer-deps=true
